{
  "installCommand": "echo skipping npm install",
  "buildCommand": "echo done",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ],
  "crons": [
    {
      "path": "/api/cron?action=monthly-coops",
      "schedule": "0 22 1 * *"
    }
  ]
}
